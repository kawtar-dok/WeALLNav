@model IEnumerable<WeALLNav.Models.Line_Demande>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section class="hero-wrap hero-wrap-2 js-fullheight" style="background-image: url(../../images/bgg1.jfif);" data-stellar-background-ratio="0.5">
    <div class="overlay"></div>
    <div class="container">
        <div class="row no-gutters slider-text js-fullheight align-items-end justify-content-start">
            <div class="col-md-9   pb-5">
                <p class="breadcrumbs"><span class="mr-2"><a href="index.html">Home <i class="ion-ios-arrow-forward"></i></a></span> <span>Demande Navette Client<i class="ion-ios-arrow-forward"></i></span></p>
                <h1 class="mb-3 bread">Demande Navette Client</h1>
            </div>
        </div>
    </div>
</section>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nbr_Demande)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Demande_Navette_Clt.Ville_Depart)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nbr_Demande)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Demande_Navette_Clt.Ville_Depart)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id_line_Cmd }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id_line_Cmd }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id_line_Cmd })
            </td>
        </tr>
    }

</table>
